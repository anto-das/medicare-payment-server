model Categories {
    category_id          String     @unique @default(uuid())
    category_type        String     @default("ANTIBIOTICS")
    category_description String     @default("")
    medicines            Medicine[]
}
